# Your domain name (required for Docker deployment).
# DNS must have an A record pointing to this server's IP.
HOOKD_DOMAIN=hookd.example.com

# Admin token for channel management (create/delete).
# If not set, these endpoints are unrestricted (fine for local dev).
# For production, generate one: openssl rand -hex 32
# HOOKD_ADMIN_TOKEN=
